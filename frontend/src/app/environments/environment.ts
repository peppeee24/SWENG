// Definizione dell'oggetto di configurazione per l'ambiente di sviluppo (local)
export const environment = {
  // Indica che questa configurazione è destinata all'ambiente di sviluppo (non produzione)
  production: false,
  // URL base dell'API backend da utilizzare in locale durante lo sviluppo
  apiUrl: 'http://localhost:8080/api'
};
