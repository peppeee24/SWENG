// Definizione dell'oggetto di configurazione per l'ambiente di produzione
export const environment = {
  // Indica che questa configurazione è destinata all'ambiente di produzione
  production: true,
  // URL base dell'API backend da utilizzare in produzione
  apiUrl: 'https://your-production-api-url.com/api'
};
